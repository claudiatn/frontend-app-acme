replicaCount: 1

deploymentColor: active


image:
  repository: ghcr.io/claudiatn/acme-frontend
  tag: "1.0.2"
  pullPolicy: IfNotPresent

imagePullSecrets: 
  - name: ghcr-secret

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: ""
  hosts:
    - host: frontend.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources: {}
nodeSelector: {}
tolerations: []
affinity: {}

canary:
  enabled: true
  replicas: 1
  weight: 10   # 10% del tráfico
